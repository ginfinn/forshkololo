#include <iostream>
#include <fstream>

using namespace std;
int main()
{
	setlocale(0, "");
	ifstream in("E:\\files\\input.txt");
	char a[255];
	while (in) {
		in.getline(a,255);
		cout << a << endl;
	}
	in.close();
	system("pause");
	return 0;
}
